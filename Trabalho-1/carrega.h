#include <stdio.h>
#include "geometric_types.h"
#include "geometric_function.h"

circulo novaEntrada(){
    ponto_plano ponto[3];
    int i;
    for(i = 0; i < 3; ++i) {
        scanf("%ld%ld", &ponto[i].x, &ponto[i].y);
    }

}
